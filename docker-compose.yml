version: "3.9"

services:
    backend:
     build:
       context: ./backend
     ports:
       - "5000:5000"

     container_name: task-backend

    frontend:
      build: 
         context: ./frontend
      ports:
        - "5173:5173"
      depends_on:
        - backend
      container_name: task-frontend    
